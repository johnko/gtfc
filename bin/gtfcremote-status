#!/bin/sh

# Compares the last git log of gitsync in HOSTS

for i in ${HOSTS} ; do
    printf '%s ' $i
    for k in `seq ${#i} 10` ; do
        printf ' ';
    done
    ssh urep@$i 'cd gitcluster && git log --oneline --decorate -1 | sed "s/master//g;s:/,::g"'
done
